'use client';

import styled from 'styled-components';

export default function Header() {
  return (
    <Wrapper>
      <Container>CodrenForum</Container>
      <Container></Container>
    </Wrapper>
  );
}

const Wrapper = styled.header``;

const Container = styled.section``;
